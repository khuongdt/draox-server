<UserControl x:Class="DraoxDemo.Views.ClanDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:conv="clr-namespace:DraoxDemo.Converters"
             Background="{StaticResource BrushElevated}">

    <UserControl.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolVis"/>
        <conv:InverseBoolToVisibilityConverter x:Key="InvBoolVis"/>
        <conv:StringNotEmptyToVisibilityConverter x:Key="StrVis"/>
    </UserControl.Resources>

    <DockPanel>
        <!-- Header -->
        <Border DockPanel.Dock="Top" Background="{StaticResource BrushSurface}"
                BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,1" Padding="16,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="12"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Content="← Back" Command="{Binding BackCommand}"
                        Style="{StaticResource ButtonGhost}" Padding="10,6"/>
                <!-- Tag badge -->
                <Border Grid.Column="2" Background="{StaticResource BrushAccent}" CornerRadius="6"
                        Width="40" Height="40">
                    <TextBlock Text="{Binding Clan.Tag}" FontSize="13" FontWeight="Bold"
                               Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <!-- Clan info -->
                <StackPanel Grid.Column="4" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Clan.Name}" Style="{StaticResource LabelBold}" FontSize="16"/>
                    <TextBlock Foreground="{StaticResource BrushTextMuted}" FontSize="11">
                        <Run Text="{Binding Members.Count}"/>
                        <Run Text="/50 members"/>
                    </TextBlock>
                </StackPanel>
                <!-- Action buttons -->
                <StackPanel Grid.Column="5" Orientation="Horizontal">
                    <Button Content="👥 Invite" Command="{Binding OpenInviteCommand}"
                            Style="{StaticResource ButtonGhost}" Padding="12,6" Margin="0,0,8,0"/>
                    <Button Content="Leave" Command="{Binding LeaveCommand}"
                            Style="{StaticResource ButtonGhost}" Padding="12,6" Margin="0,0,8,0"/>
                    <Button Content="Delete" Command="{Binding DeleteCommand}"
                            Style="{StaticResource ButtonGhost}" Padding="12,6">
                        <Button.Foreground>
                            <SolidColorBrush Color="#ED4245"/>
                        </Button.Foreground>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Error banner -->
        <Border DockPanel.Dock="Top" Background="#33ED4245" Padding="12,6"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StrVis}}">
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource BrushDanger}" FontSize="12"/>
        </Border>

        <!-- Two-column layout: Members | Channels -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="240"/>
                <ColumnDefinition Width="1"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Members panel -->
            <DockPanel Grid.Column="0" Background="{StaticResource BrushSurface}">
                <Border DockPanel.Dock="Top" Padding="16,10,16,6"
                        BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,1">
                    <TextBlock Text="Members" Style="{StaticResource LabelBold}"/>
                </Border>
                <TextBlock DockPanel.Dock="Top" Text="Loading..." Margin="16,8"
                           Foreground="{StaticResource BrushTextMuted}"
                           Visibility="{Binding IsLoading, Converter={StaticResource BoolVis}}"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Members}" Margin="0,4">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="12,8" Margin="4,1" CornerRadius="4">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{StaticResource BrushHover}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding RoleIcon}" FontSize="14" Margin="0,0,8,0"
                                                   VerticalAlignment="Center"/>
                                        <StackPanel VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Username}" Style="{StaticResource LabelNormal}"/>
                                            <TextBlock Text="{Binding RoleLabel}" FontSize="11"
                                                       Foreground="{StaticResource BrushTextMuted}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </DockPanel>

            <!-- Divider -->
            <Border Grid.Column="1" Background="{StaticResource BrushBorder}"/>

            <!-- Channels panel -->
            <DockPanel Grid.Column="2">
                <Border DockPanel.Dock="Top" Padding="16,10,16,6"
                        BorderBrush="{StaticResource BrushBorder}" BorderThickness="0,0,0,1">
                    <TextBlock Text="Channels" Style="{StaticResource LabelBold}"/>
                </Border>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="0,4">
                        <ItemsControl ItemsSource="{Binding Channels}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Padding="14,10" Margin="8,2" CornerRadius="6">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource BrushHover}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="28"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Grid.Column="0" Text="{Binding TypeIcon}"
                                                       FontSize="14" Foreground="{StaticResource BrushTextMuted}"
                                                       VerticalAlignment="Center"/>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                <TextBlock Text="{Binding Name}" Style="{StaticResource LabelNormal}"/>
                                                <TextBlock Text="{Binding Description}" FontSize="11"
                                                           Foreground="{StaticResource BrushTextMuted}"
                                                           Visibility="{Binding Description, Converter={StaticResource StrVis}}"/>
                                            </StackPanel>
                                            <Button Grid.Column="2" Content="Open Chat"
                                                    Command="{Binding DataContext.OpenChatCommand,
                                                        RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                    CommandParameter="{Binding}"
                                                    Style="{StaticResource ButtonAccent}" Padding="10,5"/>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </ScrollViewer>
            </DockPanel>
        </Grid>
    </DockPanel>
</UserControl>
